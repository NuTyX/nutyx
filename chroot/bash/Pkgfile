version=5.1
source=(http://ftp.gnu.org/gnu/bash/bash-$version.tar.gz)
build()
{
cd bash-$version

./configure --prefix=/tools \
 --without-bash-malloc

make

make install

ln -vsf bash /tools/bin/sh
mkdir  $NTC/bin
ln -vsf /tools/bin/bash $NTC/bin/bash
ln -vsf  bash $NTC/bin/sh
}
